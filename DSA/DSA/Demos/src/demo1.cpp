#include <iostream>
#include "../include/test1.cpp"

using namespace std;

int main()
{
      int value; // variable value it's have only address but it's not assgined yet
      input(&value);
      output(&value);
      return 0;
}